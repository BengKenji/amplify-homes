type SuperStar @model @auth(rules: [{allow: public}]) {
  id: ID!
  image_url: String
  name: String
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 